<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StatUS_Patient_Home" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StatUS_Patient_Home" parent="Base.Theme.StatUS_Patient_Home" />

    <!-- Base application theme. -->
    <style name="AlertDialogCustom" parent="@style/ThemeOverlay.AppCompat.Dialog.Alert">
        <!-- Text color -->
        <item name="android:textColor">@color/pale_blue</item>
        <!-- Message box color -->
        <item name="android:textColorPrimary">@color/pale_blue</item>
        <!-- Background box color -->
        <item name="android:background">@color/purple_200</item>
        <!-- Text size -->
        <item name="android:textSize">20sp</item>
    </style>
</resources>